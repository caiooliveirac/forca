DB_NAME=galgenspiel
DB_USER=galgen
DB_PASSWORD=TROCAR_PARA_SENHA_FORTE
# ⚠ Usar apenas caracteres URL-safe na senha (sem / + = etc.)
#   Gerar com: openssl rand -hex 24
JWT_SECRET=TROCAR_PARA_STRING_ALEATORIA_LONGA
CORS_ORIGIN=http://localhost:5173

# Produção (docker-compose.prod.yml)
# CORS_ORIGIN=https://seudominio.com
# BACKEND_PORT=3077
# VITE_API_URL=/forca
# VITE_BASE=/forca/

# Override docker-nginx (docker-compose.prod.override.docker-nginx.yml)
# SHARED_NETWORK_NAME=perguntas_default
